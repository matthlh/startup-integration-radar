# ─── Integration Radar — Signal Configuration ────────────────────────────────
#
# This file controls everything about how companies are scored.
# You do not need to touch any Python files to tune detection.
#
# HOW SCORING WORKS
# -----------------
# Each company page is scanned for keywords.
# Every keyword hit adds to that signal's score bucket.
# Score buckets are capped at max_points (saturating: 1 hit = clue, 3+ = strong).
# weight multiplies the bonus when many hits accumulate on the same signal.
# Total score = sum of all signal buckets, capped at 100.
# Disqualifier keywords subtract 35 points and may trigger disqualification.
#
# TOTAL MAX SCORE = sum of all max_points below (should equal ~100).
# ─────────────────────────────────────────────────────────────────────────────

# Name of your company — used in outreach email templates and demo concepts.
your_company_name: "Integration Scout"

# Scoring profile metadata — stamped into every Clay export row so you know
# which version of the rules produced a given score.
scoring_profile_name: "Integration Scout v1"
scoring_rules_version: "1.0"

# ─── Signals ─────────────────────────────────────────────────────────────────
# max_points: most this signal can contribute to the total score
# weight:     bonus multiplier when the same signal appears many times
# keywords:   any of these found on the page triggers this signal
signals:

  # The company sells a product with a workflow object (claims, orders, tickets…)
  # that customers would want to move into other systems.
  workflow_product:
    max_points: 15
    weight: 2
    keywords:
      - workflow
      - automate
      - automation
      - operations
      - platform
      - dashboard
      - review
      - approval
      - claim
      - inspection
      - quote
      - order
      - case
      - ticket
      - dispatch
      - onboarding
      - implementation
      - process

  # The company exposes an API, webhooks, or SDK — a sign that integration
  # is already part of the product surface.
  developer_surface:
    max_points: 15
    weight: 4
    keywords:
      - api
      - apis
      - sdk
      - developer
      - developers
      - docs
      - documentation
      - webhook
      - webhooks
      - sandbox
      - endpoint
      - graphql
      - rest api

  # The company explicitly talks about integrations, connectors, or named tools
  # like Salesforce — the strongest direct signal.
  integration_language:
    max_points: 15
    weight: 4
    keywords:
      - integration
      - integrations
      - connect
      - connected
      - sync
      - import
      - export
      - push data
      - pull data
      - salesforce
      - hubspot
      - slack
      - sap
      - netsuite
      - quickbooks
      - workday
      - snowflake
      - jira
      - procore
      - autodesk
      - stripe

  # Enterprise sales motion implies larger deals with more integration requirements.
  enterprise_motion:
    max_points: 10
    weight: 2
    keywords:
      - enterprise
      - security
      - soc 2
      - sso
      - role-based
      - rbac
      - audit
      - implementation
      - procurement
      - compliance
      - customers
      - case study

  # Customer systems (CRM, ERP, etc.) create integration pain for the company's users.
  customer_system_complexity:
    max_points: 15
    weight: 3
    keywords:
      - erp
      - crm
      - dms
      - dealer management
      - claims management
      - fleet management
      - transportation management
      - warehouse
      - ehr
      - emr
      - billing
      - accounting
      - procurement
      - project management
      - data warehouse
      - legacy system

  # Professional services or heavy onboarding implies custom integration work is needed.
  implementation_burden:
    max_points: 10
    weight: 2
    keywords:
      - custom
      - customize
      - configure
      - implementation
      - migration
      - onboarding
      - professional services
      - solution engineer
      - customer success
      - deployment
      - rollout

  # Partner or marketplace ecosystem implies external system dependencies.
  partner_ecosystem:
    max_points: 5
    weight: 2
    keywords:
      - partner
      - partners
      - marketplace
      - app marketplace
      - ecosystem
      - reseller
      - technology partner
      - solution partner

  # Funding or growth signals can increase integration backlog pressure.
  urgency_or_growth:
    max_points: 5
    weight: 1
    keywords:
      - series a
      - series b
      - hiring
      - we're hiring
      - scale
      - growing
      - launched
      - new funding
      - backed by
      - yc
      - a16z
      - sequoia
      - founders fund

  # A clear workflow object or demo makes it easier to build a concrete pitch.
  demoability:
    max_points: 10
    weight: 2
    keywords:
      - demo
      - request demo
      - book a demo
      - case study
      - customer story
      - use case
      - before and after
      - template
      - example
      - sample
      - calculator
      - report

  # A competitor signal is a strong buying signal — they already care about this space.
  competitor_presence:
    max_points: 10
    weight: 5
    keywords:
      - merge.dev
      - merge api
      - merge unified api
      - useparagon
      - paragon
      - paragon embedded
      - paragon integration
      - unified api provider

  # These patterns suggest the company is NOT a B2B integration prospect.
  # Each hit subtracts 35 points from the total score.
  disqualifier:
    max_points: 100
    weight: -10
    keywords:
      - consumer app
      - mobile game
      - media company
      - newsletter
      - agency only
      - consulting only
      - ecommerce store
      - restaurant
      - local service
      - personal blog

# ─── Personas ────────────────────────────────────────────────────────────────
# Controls who to contact, including the routing rules.
# - small_company_threshold: companies below this employee count get the founder persona.
# - Each persona below has its title list, rationale, and priority.
# - persona_rules (further below) controls which personas fire under which conditions —
#   change them to retarget without editing Python.
personas:
  small_company_threshold: 50

  founder:
    titles:
      - Founder
      - CEO
      - Co-founder
    why: "Founder owns integration tradeoffs directly at small companies."
    priority: 5

  product:
    titles:
      - Head of Product
      - VP Product
      - Product Lead
      - Product Manager, Integrations
    why: "Product likely owns roadmap tradeoffs when integrations block deals."
    priority: 5

  partnerships:
    titles:
      - Head of Partnerships
      - Partnerships Lead
      - Ecosystem Lead
    why: "Partnerships may own integration coverage and ecosystem expansion."
    priority: 4

  engineering:
    titles:
      - CTO
      - Head of Engineering
      - Engineering Manager, Platform
      - Integrations Lead
    why: "Engineering feels the maintenance cost of one-off customer integrations."
    priority: 4

  solutions:
    titles:
      - Head of Solutions
      - Solutions Engineering Lead
      - Implementation Lead
    why: "Solutions teams see integration friction during onboarding and enterprise pilots."
    priority: 4

# ─── Persona routing rules ────────────────────────────────────────────────────
# Each rule fires when its condition matches. Matching personas are added to the
# recommendation list, deduplicated by name; the first matching primary rule
# wins, the rest are added as secondaries.
#
# Condition shapes:
#   employee_count: { lt: 50 }       — strictly less than 50
#   employee_count: { gte: 50 }      — 50 or more
#   employee_count: unknown          — employee count is None
#   any_signal: [signal_name, ...]   — any of these signals has positive points
#
# The `reason` is appended verbatim to persona_reasoning so the operator
# understands why each persona was suggested.
persona_rules:
  primary:
    - name: small_company_founder
      condition:
        employee_count: { lt: 50 }
      persona: founder
      reason: "Company appears to have fewer than 50 employees; founders own integration tradeoffs directly at this size."

    - name: scaled_company_product
      condition:
        employee_count: { gte: 50 }
      persona: product
      reason: "Company appears to have 50+ employees; Product typically owns integration roadmap tradeoffs at this size."

    - name: unknown_size_default
      condition:
        employee_count: unknown
      persona: product
      reason: "Headcount is unknown; defaulting to Product as the most common owner of integration roadmap."

  secondary:
    - name: scaled_company_partnerships
      condition:
        employee_count: { gte: 50 }
      persona: partnerships
      reason: "At 50+ employees, Partnerships is often a parallel owner of integration coverage."

    - name: partnership_language_signal
      condition:
        any_signal: [partner_ecosystem, integration_language, customer_system_complexity]
      persona: partnerships
      reason: "Public partnership/ecosystem language was detected, so Partnerships is likely an active stakeholder."

    - name: developer_surface_signal
      condition:
        any_signal: [developer_surface, implementation_burden]
      persona: engineering
      reason: "API, SDK, webhook, or developer-docs language was detected, so Engineering owns the integration surface."

    - name: developer_surface_solutions
      condition:
        any_signal: [developer_surface, implementation_burden]
      persona: solutions
      reason: "Heavy developer surface or implementation burden — Solutions teams feel the onboarding friction."

  fallback:
    - name: unknown_or_weak_founder_fallback
      condition:
        employee_count: unknown
      persona: founder
      reason: "When company size is unknown, founder outreach is a useful validation channel."
      override_why: "Use founder outreach to validate whether integration pain is a real priority when company size is unknown or public signals are weaker."
      override_priority: 3

    - name: weak_score_founder_fallback
      condition:
        score_lt: 70
      persona: founder
      reason: "Public integration signals are weaker (score < 70); founder outreach can validate priority directly."
      override_why: "Use founder outreach to validate whether integration pain is a real priority when company size is unknown or public signals are weaker."
      override_priority: 3

# ─── Categories ───────────────────────────────────────────────────────────────
# Used to label a company's vertical in the dashboard and CSV.
# Checked in order against homepage text — first match wins.
# If nothing matches, falls back to "vertical AI / workflow automation"
# (when 'ai' or 'automation' is present) or "B2B workflow software".
categories:
  - name: automotive
    keywords: [automotive, vehicle, dealer, fleet, inspection]
  - name: construction tech
    keywords: [construction, bim, contractor, subcontractor]
  - name: sales automation
    keywords: [sales engagement, sales automation, prospect, outbound sales]
  - name: finance operations
    keywords: [finance, invoice, payment, accounting, bookkeeping]
  - name: healthcare operations
    keywords: [health, patient, clinic, care, medical, ehr]
  - name: logistics
    keywords: [logistics, freight, dispatch, warehouse, shipping]
  - name: legal tech
    keywords: [legal, contract, matter, law firm, litigation]
  - name: HR tech
    keywords: [hris, payroll, hiring, recruiting, workforce]
  - name: property tech
    keywords: [property, real estate, landlord, tenant, leasing]

# ─── Competitors to watch ─────────────────────────────────────────────────────
# When any alias is found on a company page, a competitive_trigger is created.
# Add any integration platform or competitor you want to detect.
# The display name (e.g. "Merge.dev") is what appears in reports and outreach.
competitors:
  "Merge.dev":
    aliases:
      - merge.dev
      - merge api
      - merge unified api
  "Paragon":
    aliases:
      - useparagon
      - paragon
      - paragon embedded
      - paragon integration
